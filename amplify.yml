version: 1
frontend:
  runtime-versions:
    nodejs: 20
  phases:
    preBuild:
      commands:
        - cd crm-frontend
        - npm ci
    build:
      commands:
        - cd crm-frontend       # <-- make sure we’re inside crm-frontend again
        - npm run build
  artifacts:
    baseDirectory: crm-frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - crm-frontend/node_modules/**/*